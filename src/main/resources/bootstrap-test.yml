spring:
  cloud:
    nacos:
      config:
        server-addr: # yours
        namespace: # yours
        group: # yours
        prefix: auth-center
        file-extension: yaml
      discovery:
        server-addr: # yours
        namespace: # yours
