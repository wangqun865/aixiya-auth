spring:
  cloud:
    nacos:
      config:
        server-addr: # yours
        group: # yours
        prefix: auth-center
        file-extension: yaml
        namespace: # yours
      discovery:
        namespace: # yours
        server-addr: # yours